# Design: Lingo — Self-Hosted Company Glossary

Generated by /office-hours on 2026-03-25
Status: DRAFT
Mode: Builder

## Problem Statement

Every company accumulates a dense vocabulary of internal acronyms, service names, project
codenames, and org-specific phrases. New hires spend weeks confused. Existing employees
waste time answering the same "what does X mean?" questions. Existing solutions (Confluence,
Notion, Google Docs) fail because they are **not in the flow** — you have to leave where you
encountered the term, go find the doc, hope it's up to date, and come back.

Lingo makes the glossary live where you work: Slack, CLI, and AI agents.

## What Makes This Cool

- **In-flow lookup:** Type `/lingo define BART` in Slack and get the answer without leaving the conversation.
- **AI-native:** An MCP endpoint means any AI agent (Claude, Cursor, Zed) can automatically
  resolve internal terms it encounters in code, docs, or tickets.
- **Community-legitimized:** Terms earn "Official" status through votes — no gatekeeper bottleneck,
  but editors can fast-track important terms.
- **Self-hostable in under 5 minutes:** Docker-compose, plug in your IdP, you're done.

## Constraints

- Must support Google Workspace, Okta, or any SAML-compatible IdP for auth
- Must be self-hostable (Docker/docker-compose); no SaaS dependency
- Accessible to non-developers (web UI), developers (CLI), and AI agents (MCP/REST API)
- Slack integration must work with Slack's existing OAuth flow (requires workspace admin approval)
- **Note on setup time:** OIDC/SSO configuration requires IdP admin access and app registration, which may take hours to days depending on the organization. The "5-minute" promise applies once SSO credentials are in hand. A `LINGO_DEV_MODE=true` flag enables a local password auth bypass for evaluation/development; this mode is explicitly disabled in production and shows a warning banner in the web UI.

## Premises

1. **Accessibility-in-the-flow is the core insight.** The right place to look up a term
   is where you encounter it — not a separate wiki tab.

2. **API-first architecture.** One canonical data store with a REST + MCP API.
   Slack bot, web UI, and CLI are all thin clients over this API.

3. **"Official" status has two paths:**
   - Community path: reaching a vote threshold (e.g., 3+ thumbs = Community, 10+ = Official)
   - Editor path: designated editors can mark terms Official directly
   Both paths result in the same "Official" state. Editors are assigned by admins.

4. **Self-hostable means Docker/docker-compose, <5 min deploy.**
   Minimal dependencies. No managed cloud required.

5. **Auth via SSO only.** Google Workspace, Okta, or generic SAML/OIDC.
   No username/password auth. If you're not in the IdP, you can't access it.

## Approaches Considered

### Approach A: API + CLI Core Only
Minimal scope. REST + MCP API with a CLI wrapper. Slack bot and web UI deferred.
- Effort: S | Risk: Low
- Ships fastest, but leaves out the highest-adoption surface (Slack)

### Approach B: Full Stack — API + CLI + Slack Bot + Web UI (CHOSEN)
Same API core, but ships Slack bot (/lingo define, /lingo add, /lingo vote), web UI
(browse, search, vote, add terms), and CLI. Docker-compose bundles everything.
- Effort: M | Risk: Medium
- Full adoption from day one; every role can participate

### Approach C: MCP-First
MCP as the primary interface, CLI as thin wrapper, Slack calls MCP.
- Effort: M | Risk: Low-Medium
- Forward-looking but over-indexes on AI agents at the expense of human UX

## Recommended Approach

**Approach B — Full Stack.**

The wow moment is both Slack AND AI agent integration. Approach B delivers both.
The MCP endpoint is an additional route on the same REST API — it adds minimal
complexity but completes the vision. The three surfaces (web, Slack, CLI) share
the same data layer and auth.

**Tech stack recommendation:**
- **Backend:** Python 3.12 + FastAPI — async, type-annotated, OpenAPI docs for free
- **Package manager:** uv — fast lockfile-based dependency management; no pip/venv ceremony
- **Database:** Postgres (asyncpg driver via SQLAlchemy 2.0 async) — native FTS, proper concurrency
- **ORM/query layer:** SQLAlchemy 2.0 (async) — type-safe queries; Alembic for schema migrations
- **Auth:** Authlib — OIDC/SAML middleware; supports Google Workspace, Okta, generic OIDC
- **Slack:** slack-bolt (Python) `AsyncApp` in Socket Mode — no public URL required, works behind firewalls; static bot token (`SLACK_BOT_TOKEN` env var, no `InstallationStore` needed — single-workspace bot); lifespan shutdown: `asyncio.shield(handler.close_async())` (workaround for bolt-python #1112)
- **MCP:** FastMCP (`fastmcp`) + `mcp` Python SDK — Streamable HTTP transport (SSE deprecated); mounted via `streamable_http_app()` on FastAPI with combined lifespan; Bearer token auth
- **CLI:** `lingo` CLI — Typer (Click-based, type-annotated); ships as a pip-installable entry point
- **Frontend:** React + Vite — compiled to `src/lingo/static/`, served by FastAPI as static files; Tailwind CSS + shadcn/ui
- **Scheduler:** APScheduler 3.x `AsyncIOScheduler` — in-process cron for Discovery and Staleness jobs; **deploy with `--workers 1`** (multi-worker deployments fire duplicate jobs — duplicate Slack DMs)
- **Deploy:** Docker + docker-compose (lingo + postgres); Helm chart for Kubernetes; `python:3.12-slim` base image; `CMD ["uvicorn", "lingo.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1"]`

**Status model:**
```
Suggested (auto-discovered from Slack, not yet human-reviewed)
  ↕ Pending   (promoted by editor, or user-created; 0 to LINGO_COMMUNITY_THRESHOLD-1 votes)
    ↕ Community  (LINGO_COMMUNITY_THRESHOLD+ votes OR editor approval)
      ↕ Official   (LINGO_OFFICIAL_THRESHOLD+ votes OR editor marks official)
```
Thresholds are configurable via environment variables (defaults: community=3, official=10).
Tune per org size — a 10-person team may use 2/5; a large org may use 5/25.
Staleness checks exclude `suggested` terms.
**All status transitions are reversible by editors.** Demotions require a `change_note` and create a `Term_History` record. Votes are not reversed on demotion (vote count is preserved).

**Voting mechanics:**
- Any Member can upvote a term (one vote per user per term, enforced at DB level).
- `POST /api/v1/terms/:id/dispute` — notifies term owner via Slack DM.

**Role model:**
- **Member** — can add terms, vote, browse, claim ownership of unowned terms
- **Editor** — can mark terms Official, edit any term (including suggested), delete terms, manage relationships, promote/dismiss suggestions
- **Admin** — can assign Editor roles, manage SSO config, create/revoke API tokens

## Data Model

All IDs are UUID v4.

```
Term {
  id                uuid PK
  name              text NOT NULL
  full_name         text
  definition        text NOT NULL CHECK (length(definition) <= 2000)
  category          text
  status            text NOT NULL  -- pending | community | official | suggested
  source            text NOT NULL  -- 'user' | 'slack_discovery'
  source_channel_id text            -- nullable; slack channel ID with highest occurrence (slack_discovery only)
  occurrences_count int             -- only set for suggested terms
  owner_id          uuid FK → User  -- nullable
  owned_at          timestamptz
  is_stale          boolean NOT NULL DEFAULT false
  last_confirmed_at timestamptz     -- defaults to created_at; resets on /confirm
  version           int NOT NULL DEFAULT 1  -- optimistic locking; PUT requires matching version
  created_by        uuid FK → User
  created_at        timestamptz
  updated_at        timestamptz
  search_vector     tsvector GENERATED ALWAYS AS (
    to_tsvector('english',
      coalesce(name,'') || ' ' || coalesce(full_name,'') || ' ' || coalesce(definition,'')
    )
  ) STORED
}
INDEX terms_search_vector_idx ON terms USING GIN (search_vector)
-- Fuzzy search via pg_trgm (CREATE EXTENSION pg_trgm in initial migration)
INDEX terms_name_trgm_idx ON terms USING GIN (name gin_trgm_ops)
-- "Did you mean?" query: SELECT name FROM terms WHERE similarity(name, $query) >= 0.3 ORDER BY similarity DESC LIMIT 3

Term_Relationship {
  id               uuid PK
  term_id          uuid FK → Term NOT NULL
  related_term_id  uuid FK → Term NOT NULL
  relationship_type text NOT NULL  -- depends_on | supersedes | related_to
  created_by       uuid FK → User  -- editor+
  created_at       timestamptz
}

Term_History {
  id          uuid PK
  term_id     uuid FK → Term NOT NULL
  definition  text
  full_name   text
  category    text
  owner_id    uuid
  status      text
  changed_by  uuid FK → User NOT NULL
  changed_at  timestamptz NOT NULL
  change_note text  -- optional, max 280 chars; from PUT body
}

Vote {
  term_id    uuid FK → Term
  user_id    uuid FK → User
  created_at timestamptz
  PRIMARY KEY (term_id, user_id)  -- enforces one vote per user per term
}

User {
  id            uuid PK
  email         text NOT NULL UNIQUE
  display_name  text
  slack_user_id text  -- nullable; populated on first Slack interaction
  role          text NOT NULL  -- member | editor | admin
  last_login_at timestamptz
  is_active     boolean NOT NULL DEFAULT true
  created_at    timestamptz
}

Token {
  id           uuid PK
  user_id      uuid FK → User
  name         text        -- human label, e.g. "cursor-agent"
  token_hash   text        -- sha256(raw_token); raw token shown once at creation
  scopes       text[]      -- ["read"] for MCP tokens
  created_at   timestamptz
  last_used_at timestamptz
  revoked_at   timestamptz  -- null = active
}
-- Token generation: crypto/rand 32 bytes → base64url
-- Token storage: sha256(token) — fast lookup, no bcrypt latency on MCP calls

Job {
  id            uuid PK
  job_type      text NOT NULL  -- 'discovery' | 'staleness'
  status        text NOT NULL  -- pending | running | completed | failed
  progress_json jsonb           -- { channels_done, channels_total, cursor }
  started_at    timestamptz
  completed_at  timestamptz
  error         text
}
```

## API Routes

All REST routes are prefixed `/api/v1/`. The MCP endpoint follows the MCP spec and is unversioned.

```
-- Terms
GET    /api/v1/terms                           — list/search (?q=, ?status=, ?category=)
                                                 returns { items, total, offset, limit, counts_by_status } envelope
POST   /api/v1/terms                           — add a term (member+)
GET    /api/v1/terms/:id                       — get term (includes relationships[], linked_terms[])
PUT    /api/v1/terms/:id                       — update term; requires `version` field (optimistic lock);
                                                 triggers Term_History snapshot;
                                                 member for own user-created terms; editor+ for others + all suggested
DELETE /api/v1/terms/:id                       — delete (editor+)
POST   /api/v1/terms/:id/vote                  — upvote (member+; transactional; deduped at DB level)
POST   /api/v1/terms/:id/dispute               — dispute/downvote (member+; notifies owner via Slack DM)
POST   /api/v1/terms/:id/official              — mark official (editor+)
POST   /api/v1/terms/:id/confirm               — confirm not stale (owner or editor+; resets last_confirmed_at)
POST   /api/v1/terms/:id/claim                 — claim ownership (member+ if owner_id is null; editor+ to override)
GET    /api/v1/terms/:id/history               — edit history
POST   /api/v1/terms/:id/revert/:history_id    — revert to snapshot (editor+)
POST   /api/v1/terms/:id/relationships         — add relationship (editor+)
DELETE /api/v1/terms/:id/relationships/:rel_id — delete relationship (editor+)
POST   /api/v1/terms/:id/promote               — suggested → pending (editor+)
POST   /api/v1/terms/:id/dismiss               — discard suggestion (editor+)

-- Export
GET    /api/v1/export                          — export Official terms as markdown
                                                 ?status=official&format=markdown
                                                 ?offset= for pagination (no hard cap — paginate properly)

-- Users
GET    /api/v1/users                           — list users (admin only)
PATCH  /api/v1/users/:id/role                  — set role: member|editor|admin (admin only)

-- API Tokens (for MCP auth)
GET    /api/v1/tokens                          — list tokens for current user
POST   /api/v1/tokens                          — create token (admin only)
DELETE /api/v1/tokens/:id                      — revoke token (admin only)

-- Admin operational surface
GET    /api/v1/admin/stats                     — glossary stats (admin only)
GET    /api/v1/admin/jobs                      — list background jobs (admin only)
POST   /api/v1/admin/jobs/:type/run            — trigger job manually: discovery | staleness (admin only)

-- MCP
GET    /mcp                                    — MCP endpoint for AI agents (Bearer token auth)
                                                 tools: get_term, search_terms, list_terms
```

## MCP Tool Signatures

```
get_term(name: string) → {
  id, name, full_name, definition, category, status,
  owner: { display_name } | null,
  relationships: [{ type, related_term: { name, definition } }],
  linked_terms: [{ id, name }]
}

search_terms(query: string, status?: string, limit?: number) → {
  terms: [{ id, name, full_name, definition, status }]
}

list_terms(category?: string, status?: string, limit?: number, offset?: number) → {
  terms: [{ id, name, full_name, definition, status }],
  total: number
}
```

## Required Slack OAuth Scopes

```
commands               — slash commands (/lingo define, add, vote, export)
chat:write             — post messages + DMs to term owners
files:write            — file upload for /lingo export
users:read             — resolve user display names
users:read.email       — match Slack users to SSO emails (for slack_user_id)
conversations:history  — auto-discovery job (must be disclosed to users during setup)
```

## Background Jobs

**LingoDiscoveryJob** (`job_type: 'discovery'`)
- Scans 90 days of Slack history per invited channel (public + private only; skips DMs)
- Rate-limited to 45 req/min (Slack Tier 3 limit)
- Async, incremental, restartable — progress stored in `Job.progress_json`
- Detection: regex `/\b[A-Z]{2,6}\b/g` on message text; surfaces term if `occurrences_count >= 3`
- Deduplication: if a term with the same name (case-insensitive) already exists at any status, updates `occurrences_count` only — no duplicate `suggested` term created
- Resulting terms have `status = suggested`, `source = slack_discovery`
- **Source channel tracking:** Store the channel with highest occurrence count as `source_channel_id` on suggested terms (add field to Term: `source_channel_id text` — nullable, populated only for slack_discovery terms)
- **Promotion notification:** When an editor promotes a `suggested` term to `pending` or higher, post a channel message to `source_channel_id`: "📚 Lingo: [TERM] has been added to the glossary! [definition preview] — Thanks to this channel for surfacing it. [View in Lingo]" (only if `source_channel_id` is set)

**LingoStalenessJob** (`job_type: 'staleness'`)
- Runs weekly (cron)
- Checks only `pending | community | official` terms (not `suggested`)
- Flags term as `is_stale = true` if `now() - last_confirmed_at > LINGO_STALE_THRESHOLD_DAYS` (default 180)
- Sends Slack DM to owner with Confirm/Update buttons
- Unowned stale terms go to admin "Needs Review" queue
- Admin notified when owner's `last_login_at > 90 days` (deprovisioning detection)

## UI Layout

### Information Architecture

**Classifier: APP UI** — task-focused, internal tool for managing/browsing glossary terms.

**Layout: Search-first, two-column**

Primary interaction: reactive lookup (user encounters a term, wants a definition fast). Browse is secondary.

```
+--------------------------------------------------+
| [LINGO] Official Glossary    [Search terms...  ] |
+--------------------------------------------------+
|                                                  |
| Status: [All] [Official] [Community] [Suggested (N)] |
| Category: [All] [Eng] [Ops] [Product]  [+ Add]  |
|------------------------------------------------- |
| BART                    ✓ Official  👍 12          |
| Business Arts Resource Tool                      |
|                                                  |
| FMTL                    ○ Community 👍 4           |
| Field Management Tool                            |
+--------------------------------------------------+
```

**Navigation hierarchy:**
1. Search bar — full-width, above-the-fold, placeholder "Search terms..."
2. Status/category filter bar — horizontal pill filters below search
3. Term list — cards sorted by status rank (Official first), then alphabetical

**Term detail — slide-in right panel:**
Clicking a term card slides in a detail panel from the right, list remains visible and interactive.

```
+--------------------------------------------------+
| [LINGO]           [Search terms...             ] |
+---------------------------+----------------------+
| Status: [All] [Official]  | BART                 |
|   [+ Add]                 | Business Arts...     |
|                           |                      |
| BART           Official   | Definition:          |
| > selected <              | A centralized hub... |
|                           |                      |
| FMTL           Community  | Status: ✓ Official   |
|                           | Votes: 12            |
| QWERTY         Pending    | Owner: @alice        |
|                           | Last confirmed: 3d ago|
|                           |                      |
|                           | Depends on: [FMTL]  |
|                           |                      |
|                           | [Vote] [Dispute]    |
+---------------------------+----------------------+
```

**Editor/Admin queue — sidebar badge:**
Status filter shows live counts. Suggested terms display a count badge that draws editor attention.

```
Status:
▣ All (104)
□ Official (62)
□ Community (21)
□ Pending (8)
□ Suggested (23)  ← count badge; editors filter here to review
```

**Admin "Needs Review" queue:** Stale unowned terms — accessible by filtering Status=All, then sorting by `is_stale=true`. Admin panel link in nav for direct access.

## Design System

**Classifier: APP UI** — internal developer tool. Calm surface hierarchy, utility language, dense but readable.

### Typography

```css
--font-primary: "Berkeley Mono", "JetBrains Mono", monospace;  /* term names, code */
--font-ui: "Inter", system-ui, sans-serif;                      /* UI chrome, body */

--text-2xl: 20px / 700;   /* page title */
--text-xl:  16px / 600;   /* term name in detail panel */
--text-base: 14px / 400;  /* term rows, body */
--text-sm:   12px / 400;  /* metadata: owner, timestamp, vote count */
--text-xs:   11px / 500;  /* status badges */
```

Term names use monospace — they're identifiers, not prose. This signals "this is a code/system term" and differentiates Lingo from a generic wiki.

### Color System

```css
/* Surfaces */
--color-bg:          #FAFAFA;   /* page background */
--color-surface:     #FFFFFF;   /* detail panel, modals */
--color-border:      #E5E7EB;   /* hairline borders */
--color-hover:       #F3F4F6;   /* row hover */

/* Text */
--color-text-primary:   #111827;
--color-text-secondary: #6B7280;
--color-text-muted:     #9CA3AF;

/* Status badges (muted, not saturated — this is utility not brand) */
--color-official:    #D1FAE5;  /* bg */ / #065F46;  /* text — green */
--color-community:   #FEF3C7;  /* bg */ / #92400E;  /* text — amber */
--color-pending:     #E5E7EB;  /* bg */ / #374151;  /* text — gray */
--color-suggested:   #F3F4F6;  /* bg */ / #9CA3AF;  /* text — light gray */
--color-stale:       #FEE2E2;  /* bg */ / #991B1B;  /* text — red */

/* Accent — single, used sparingly */
--color-accent:      #2563EB;  /* blue — links, [Vote] button, search focus ring */
--color-accent-hover: #1D4ED8;
```

### Spacing Scale

```
4px  — tight metadata gaps (vote count + icon)
8px  — row padding vertical
12px — row padding horizontal, badge padding
16px — panel content padding
24px — section gaps
32px — page padding
```

### Term List: Table Rows (not cards)

Dense rows, no card borders. Definition in detail panel only.

```
BART          ✓ Official   👍 12   Operations   3d ago
FMTL          ○ Community  👍 4    Engineering  2w ago
QWERTY        □ Pending    👍 1    Product      —
UNKNWN        • Suggested  —      —             —
```

Row anatomy: `[term name (mono, bold)] [status badge] [vote count] [category tag] [last confirmed]`

Hover: `--color-hover` background. Click: opens slide-in detail panel.

### Status Badges

Pill shape, `border-radius: 9999px`, 11px uppercase text. Colors from system above. No icons inside badges — text only (`Official`, `Community`, `Pending`, `Suggested`). Stale terms append a `⚠ Stale` chip in red.

### Frontend Stack

- **Framework:** React + Vite — compiled to `src/lingo/static/`, served by FastAPI as static files
- **CSS:** Tailwind CSS — token values map to `tailwind.config.js` CSS variables
- **Components:** shadcn/ui (Radix primitives) — Button, Badge, Dialog, Command (search), Popover
- **State:** React Query for server state (term list, search, detail); local state for optimistic updates
- **No server-side templating** — FastAPI serves the compiled bundle; all rendering is client-side

### No-card Rule

Cards (bordered boxes) are forbidden in the term list. They appear only in:
- Setup wizard steps
- Error/empty state illustrations
- The slide-in detail panel (which is a surface, not a card)

## Responsive & Accessibility

### Breakpoints

```
Mobile:  < 768px   — single column, full-screen detail on tap
Tablet:  768–1024px — two column (list + detail), filter bar as row
Desktop: > 1024px  — full layout (sidebar + list + slide-in detail)
```

### Mobile Layout (< 768px)

```
+---------------------------+
| [LINGO]     [Filters] [+] |
| [Search terms...        ] |
+---------------------------+
| BART   ✓ Official   👍 12  |
| FMTL   ○ Community  👍 4   |
| QWERTY □ Pending    👍 1   |
+---------------------------+
```

Tap a row → full-screen term detail (slide up from bottom, `< Back` to return).
Filter button opens a bottom sheet with status/category filters.
`[+]` opens add-term modal.
Touch targets: minimum 44×44px (all buttons, row items).

### Tablet Layout (768–1024px)

```
+----------------------------------+
| [LINGO]  [Search...  ] [+]       |
| [All][Official][Community][More▼]|
+------------------+---------------+
| BART   Official  | BART          |
| FMTL   Community | Definition... |
| QWERTY Pending   | [Vote]        |
+------------------+---------------+
```

Sidebar filters collapse to horizontal pill row. Detail panel fills right half.

### Keyboard Navigation

- `Tab` — move through interactive elements in order
- `Escape` — close detail panel, dismiss modals
- `↑↓` — navigate term list rows (focus moves, detail panel updates)
- `Enter` / `Space` — open selected term detail
- `Cmd+K` / `Ctrl+K` — focus search bar from anywhere (global shortcut)
- `/` — focus search bar (single-key shortcut, mirrors GitHub/Linear)

### Accessibility Requirements

- **ARIA landmarks:** `<main>`, `<nav>` (sidebar), `<search>` (search bar), `role="row"` on term rows
- **Focus management:** When detail panel opens, focus moves to panel; when closed, returns to triggering row
- **Screen readers:** Status badges use `aria-label="Status: Official"` (not just visual color)
- **Color contrast:** All text meets WCAG AA (4.5:1 minimum). Status badges tested: Official green text on green bg is `#065F46` on `#D1FAE5` = 5.9:1 ✓
- **Motion:** Slide-in panel animation respects `prefers-reduced-motion` (instant show/hide if reduced)
- **Font sizing:** Base 14px, no smaller than 11px (status badges). Users can scale with browser zoom.

## Interaction State Coverage

### Web UI State Table

| Feature | Loading | Empty | Error | Success | Partial/Edge |
|---------|---------|-------|-------|---------|-------------|
| **Term list** | Skeleton cards (3 placeholder rows, shimmer animation) | "No terms yet. Add the first one." + [Add a term] CTA | "Couldn't load terms. [Retry]" banner | — | — |
| **Search** | Skeleton cards while query in flight (debounced 200ms) | "No results for 'BARTT'. Did you mean: BART? [+ Add BARTT as new term]" — fuzzy match threshold: edit distance ≤ 2 | "Search failed. [Retry]" inline | — | — |
| **Vote** | Button shows spinner (optimistic: instant increment) | — | Rollback vote count + toast: "You already voted on this term" | Vote count increments immediately; button → [Voted ✓] disabled | Votes are one-time, no toggle. Second vote = error toast |
| **Add term** | [Add] button shows spinner | — | Inline field validation errors. API error: toast "Couldn't save. [Retry]" | Toast "Term added" (3s), panel resets, new term appears in list | Duplicate name warning inline before submit |
| **Edit term** | [Save] button shows spinner | — | Version mismatch: show conflict diff modal (your edit vs. current, with [Keep mine] / [Take theirs] / [Edit manually]) | Toast "Saved" (3s), detail panel updates | Permission denied: [Edit] button hidden for members on others' terms |
| **First load (empty DB)** | — | "Your glossary is empty. Add your first term or run discovery to import from Slack." + [Add term] + [Run discovery] CTAs | — | — | — |
| **SSO login failure** | — | — | Full-page error: "Sign-in failed. Try again or contact your admin." + [Try again] + admin contact email | — | — |
| **API down** | — | — | Banner at top: "Lingo is having trouble connecting. Your changes may not save." Persists until resolved | — | — |
| **Dispute** | — | — | Toast "Couldn't send dispute. [Retry]" | Toast "Dispute sent — term owner notified" (3s) | — |
| **Confirm staleness** | — | — | Toast "Couldn't confirm. [Retry]" | Toast "Confirmed — term marked current" (3s) | — |

### Slack Interaction States

| Command | Success | Error / Edge |
|---------|---------|-------------|
| `/lingo define BART` | Ephemeral message with term card (name, definition, status badge, vote count) | Not found: "No term 'BART'. Did you mean: BARTT? Type `/lingo add BART [definition]` to create it." |
| `/lingo add TERM DEF` | Ephemeral: "Term added as Pending. An editor will review it." | Duplicate: "A term named BART already exists. View it: [link]" |
| `/lingo vote TERM` | Ephemeral: "Voted for BART (now 13 votes)" | Already voted: "You already voted on BART." |
| `/lingo export` | DM with exported markdown file | Empty glossary: "No Official terms to export yet." |

### CLI Output States

```bash
# Success: lingo define BART
┌─ BART
├─ Business Arts Resource Tool
├─ Definition: A centralized hub for resource allocation...
├─ Status: Official (12 votes)
├─ Category: Operations
└

# Fuzzy match (no exact result): lingo define BART
No exact match for "BART" — showing closest result.
┌─ BART2
├─ ...
└

# Not found: lingo define UNKNWN
Error: no term found for "UNKNWN"

# Add a term: lingo add BART "A centralized hub for resource allocation" --full-name "Business Arts Resource Tool" --category Operations
Added: BART (status: pending)

# List terms: lingo list --status official --limit 10
 Name   Status    Votes  Definition
 BART   official  12     A centralized hub for resource...
 FMTL   official  7      Field Management Tool...

# Export to stdout: lingo export --status official
# Lingo Glossary

## BART
...

# Export to file: lingo export --status official --output glossary.md
Exported to glossary.md
```

Configuration (environment variables):
- `LINGO_APP_URL` — base URL of the Lingo server (default: `http://localhost:8000`)
- `LINGO_API_TOKEN` — Bearer token for authentication (optional in dev mode)
- `LINGO_DEV_USER_ID` — dev mode user ID header (`X-User-Id`) when `LINGO_API_TOKEN` is unset; **only accepted when `LINGO_DEV_MODE=true`** (security: header is ignored in production)

### First-Run Setup Wizard States

```
Step 1: Welcome
  Bootstrap token accepted → show setup form
  Token expired/invalid → "Bootstrap token expired. Restart Lingo to generate a new one."

Step 2: OIDC Configuration
  Fields: OIDC Issuer URL, Client ID, Client Secret
  [Test Connection] → loading spinner → "✓ Connected to Google Workspace" or error details

Step 3: Create Admin Account
  SSO login → first user auto-promoted to Admin
  On completion: "Setup complete. You're logged in as admin." → redirect to glossary

Permanent block: /setup returns 404 after first admin created.
```

## Resolved Design Decisions

These were previously open questions — now resolved:

1. **Fuzzy "Did you mean?" threshold:** Enabled. Edit distance ≤ 2. Not user-configurable in v1 (fixed at server level via `LINGO_FUZZY_THRESHOLD=2`).

2. **Slack `/lingo add` flow:** Opens a Slack modal (not inline text parsing). Command `/lingo add [TERM]` pre-fills Term field. Modal fields: Term, Full Name, Definition (multi-line), Category. Submitted to `POST /api/v1/terms`.

3. **Status reversibility:** All status transitions are reversible by editors. `PUT /api/v1/terms/:id` with `status` field changes to any status. Every status change creates a `Term_History` record; `change_note` is required when demoting (status going down). The status machine is not enforced as one-directional.

4. **Filter logic:** All filters use AND logic. `status=Official&category=Engineering` returns only Official Engineering terms. Multiple selections within the same filter group (e.g., two categories) are OR'd within the group, AND'd with other groups.

5. **Staleness admin queue:** Accessible via Status filter in main UI (filter `is_stale=true`). Admin nav bar link "Needs Review (N)" surfaces this filtered view directly.

6. **SQLAlchemy async session pattern:** Use `async_sessionmaker` + `Depends(get_db)` for request-scoped sessions. Never use `async_scoped_session` with FastAPI (DI already handles scoping). Never share a single `AsyncSession` across concurrent `asyncio.gather()` tasks — each task needs its own session. Set `expire_on_commit=False` to prevent `MissingGreenlet` errors on attribute access post-commit. DB URL must use `postgresql+asyncpg://` (not `postgresql://` — silent fallback to sync I/O blocks the event loop under load).

7. **Alembic migration execution:** Migrations run as a **separate process before the app starts** (docker-compose `command: sh -c 'alembic upgrade head && uvicorn ...'` or a Kubernetes init container). Never run `alembic upgrade head` inside FastAPI lifespan — blocks startup, and `asyncio.run()` raises `RuntimeError` if an event loop already exists. Failed migration = failed deployment (intentional — prevents app from starting against wrong schema).

8. **K8s replica constraint:** APScheduler fires once per process. Multi-replica Kubernetes deployments (replicas > 1) will fire scheduled jobs (LingoDiscoveryJob, LingoStalenessJob) once per replica, resulting in duplicate Slack DMs and duplicate DB writes. **Set `replicaCount: 1` in Helm values when running with the built-in scheduler.** Multi-replica support with distributed locking (Postgres advisory locks) is a v2 item.

9. **Vote locking strategy:** Votes use **composite PK deduplication** (INSERT raises IntegrityError for duplicate) + a **CAS UPDATE** (`UPDATE terms SET status=$new, version=version+1 WHERE id=$id AND version=$current_version AND vote_count >= threshold`). No `SELECT FOR UPDATE` on the `terms` row — avoids deadlock with optimistic locking on term edits.

10. **DEV_MODE production guard:** `LINGO_DEV_MODE=true` bypasses OIDC auth. A startup assertion in `main.py` lifespan raises `RuntimeError` if `settings.dev_mode=True` and `settings.environment='production'`. `LINGO_ENV` defaults to `'development'`; must be explicitly set to `'production'` in prod deployments.

11. **LingoStalenessJob DM rate limiting:** DMs are sent at max 30/minute (`asyncio.sleep(2)` between each) to stay within Slack's `chat.postMessage` rate limit (~1/second). This prevents bot rate-limiting on first runs against large glossaries.

12. **Bootstrap TOCTOU protection:** The `/setup` handler acquires `pg_try_advisory_xact_lock(hashtext('lingo_bootstrap'))` before checking user count. If the lock is not acquired (another request is in progress), return 503. Lock is released automatically on transaction commit/rollback.

13. **pg_trgm managed Postgres:** Initial migration uses `CREATE EXTENSION IF NOT EXISTS pg_trgm`. Managed Postgres users (RDS, Cloud SQL, Azure DB) must enable pg_trgm in their parameter group/console before deploying. This is documented in the quickstart README as a prerequisite.

## Testing Conventions

**Framework:** pytest + pytest-asyncio (`asyncio_mode = 'auto'` in pyproject.toml — no per-test decorator needed)

**API testing:** `httpx.AsyncClient` with `ASGITransport` — runs the full FastAPI app in-process against a real test DB:
```python
# tests/conftest.py
@pytest.fixture
async def client(db_session):
    async with AsyncClient(transport=ASGITransport(app=app), base_url='http://test') as ac:
        yield ac
```

**Test database:** Real Postgres only — no SQLite (hides asyncpg-specific behavior: FTS queries, `SELECT FOR UPDATE`, UUID types). Use a dedicated test DB via `LINGO_TEST_DATABASE_URL` env var. Run `alembic upgrade head` against the test DB before tests.

**Dev dependencies:** `pytest`, `pytest-asyncio`, `httpx`, `factory-boy` (fixtures), `pytest-cov`

**Critical test requirements (from coverage diagram):**
- `test_vote_concurrent_at_threshold` — 5 concurrent `asyncio.gather()` tasks voting at the threshold boundary; assert exactly 1 status transition fires
- `test_setup_blocked_after_first_admin` — `/setup` returns 404 after admin exists
- `test_staleness_job_skips_suggested` — LingoStalenessJob never sets `is_stale=True` on `suggested` terms
- `test_vote_dedup_at_db_level` — second vote from same user raises IntegrityError (Vote composite PK), caught and returned as 409

**TDD protocol (from project memory):** Red/green TDD. Write the failing test first, then implement. Use `/tdd` skill for each new feature or endpoint.

## Open Questions

- Should the CLI support offline/cached mode?
- Should Slack surface a "suggest related terms" prompt on new additions?

## Success Criteria

- A new hire can run `lingo define BART` or type `/lingo define BART` in Slack on day 1
- An AI coding agent (Claude/Cursor) automatically resolves internal terms via MCP
- Any employee can add a term in under 60 seconds (web or Slack)
- Terms reach "Official" status without requiring human admin action (voting path works)
- Deploy time: under 5 minutes with docker-compose

## Distribution Plan

- Docker Hub: `yourorg/lingo:latest`
- PyPI: `lingo-cli` package — `pip install lingo-cli` or `uvx lingo` for one-off runs
- `brew tap` for macOS CLI (stretch goal)

**Self-host quickstart (docker-compose):**
```bash
curl -O https://raw.githubusercontent.com/yourorg/lingo/main/docker-compose.yml
# Edit: set OIDC_ISSUER, SLACK_BOT_TOKEN, etc.
docker-compose up -d
```

**Self-host quickstart (Kubernetes / Helm):**
```bash
helm repo add lingo https://yourorg.github.io/lingo/charts
helm install lingo lingo/lingo \
  --set oidc.issuer=https://accounts.google.com \
  --set oidc.clientId=... \
  --set oidc.clientSecret=... \
  --set slack.botToken=... \
  --set postgres.url=postgresql://...
```

**Helm chart scope:**
- `lingo` Deployment + Service (ClusterIP)
- Optional Ingress (nginx/traefik annotations, TLS via cert-manager)
- Postgres dependency via Bitnami chart (optional — `postgres.external=true` to BYO)
- ConfigMap for non-secret env vars; Secret for tokens/credentials
- HorizontalPodAutoscaler (min 1, max 3 replicas)
- PodDisruptionBudget (minAvailable: 1)
- ReadinessProbe: `GET /health` → 200
- LivenessProbe: `GET /health` → 200
- Resource defaults: `requests: cpu=200m, memory=256Mi` / `limits: cpu=500m, memory=512Mi`
- Chart published to GitHub Pages via `helm/chart-releaser-action`

**CI/CD:**
- GitHub Actions: `uv run pytest` + lint (`ruff`) on PR, publish Docker image on merge to main
- Multi-arch Docker build (linux/amd64, linux/arm64) via `python:3.12-slim`
- PyPI publish via `uv build` + `uv publish` on release tag
- Helm chart lint + test (`helm lint`, `helm unittest`) on PR; publish chart on release tag

## First-Run Setup

On first run with an empty database, Lingo prints a one-time bootstrap token to stdout:

```
================================================
LINGO SETUP: Visit http://localhost:3000/setup
Bootstrap token: lingo-setup-a3f9b2c1d4e5
Expires in 1 hour. This message won't appear again.
================================================
```

The `/setup` route:
- Only accessible if DB has 0 users
- Accepts bootstrap token (expires 1 hour, single-use)
- Walks through: OIDC issuer URL + client_id/secret → test SSO connection → creates admin user
- Permanently blocked after first admin is created

## Next Steps

Risk-first order: prototype the two highest-risk external integrations (MCP transport, Slack Socket Mode) before building full infrastructure.

## Project Structure

```
lingo/
  src/
    lingo/
      __init__.py
      main.py           # FastAPI app + lifespan (Slack handler, APScheduler, MCP mount)
      config.py         # pydantic-settings Settings class (all env vars, validated on startup)
      api/              # thin route handlers: validate input → call service → return schema
        terms.py        # GET/POST/PUT/DELETE /api/v1/terms + term actions
        users.py        # GET /api/v1/users, PATCH role
        tokens.py       # token management
        admin.py        # stats, jobs
        export.py       # GET /api/v1/export
        auth.py         # OIDC callback routes + /setup
      models/           # SQLAlchemy ORM models (never returned directly from routes)
        term.py
        user.py
        vote.py
        token.py
        job.py
        term_history.py
        term_relationship.py
      schemas/          # Pydantic input/output schemas (serialization layer)
        term.py
        user.py
        token.py
        job.py
      services/         # business logic (called by route handlers)
        terms.py        # create_term, update_term, vote, promote, confirm_staleness, etc.
        auth.py         # OIDC token validation, session management, bootstrap token
        tokens.py       # SHA-256 hashing, token creation/revocation
      jobs/             # APScheduler job functions
        discovery.py    # LingoDiscoveryJob — Slack history scan
        staleness.py    # LingoStalenessJob — weekly stale check + DMs
      slack/            # slack-bolt AsyncApp + command handlers
        app.py          # AsyncApp initialization (static SLACK_BOT_TOKEN, no InstallationStore)
        commands.py     # /lingo define, add, vote, export handlers
      mcp_server/       # FastMCP server
        server.py       # get_term, search_terms, list_terms tools
      cli/              # Typer CLI
        main.py         # lingo define, add, search, export subcommands
      db/
        session.py      # async_sessionmaker + get_db Depends
        engine.py       # create_async_engine (postgresql+asyncpg://)
  alembic/
    env.py              # run_sync bridge; asyncio.run() in standalone script only
    versions/
  tests/
    conftest.py         # async test fixtures, test DB setup
    api/
    services/
    jobs/
    slack/
    mcp_server/
  pyproject.toml        # [project.scripts] lingo = "lingo.cli.main:app"
  Dockerfile            # python:3.12-slim; CMD uvicorn lingo.main:app --workers 1
  docker-compose.yml    # lingo + postgres services
```

1. Scaffold Python project — `uv init`, `pyproject.toml`, `src/lingo/` skeleton, Alembic config with `run_sync` bridge, all DB migrations (all tables including Job); docker-compose `command: sh -c 'alembic upgrade head && uvicorn lingo.main:app --host 0.0.0.0 --port 8000 --workers 1'`
2. **Spike: MCP endpoint** — validate `mcp` Python SDK Streamable HTTP transport (NOT SSE — deprecated Mar 2025 spec). Use FastMCP's `streamable_http_app()` mounted on FastAPI via combined lifespan (workaround for python-sdk #1367). Tool schemas, Bearer token auth with Claude/Cursor.
3. **Spike: Slack Socket Mode** — validate slash command flow with `slack-bolt`, app registration requirements, Socket Mode async lifecycle
4. Implement OIDC auth middleware via Authlib (Google OAuth first, then generalize to generic OIDC)
5. Build REST API — all `/api/v1/` routes with role enforcement + optimistic locking (SQLAlchemy 2.0 async)
6. Implement vote endpoint with DB-level transaction + row lock (P1 TODO)
7. Build bootstrap/setup wizard (`/setup` route + bootstrap token)
8. Implement Token management (SHA-256 hashing, MCP Bearer auth)
9. Implement LingoDiscoveryJob (incremental, rate-limited, restartable — APScheduler 3.x `AsyncIOScheduler`; **single Uvicorn worker required** — see scheduler note below)
10. Implement LingoStalenessJob (weekly cron via APScheduler 3.x `AsyncIOScheduler`, owner DM, admin queue)
11. Build web UI — search-first layout (React+Vite+Tailwind+shadcn) + slide-in detail panel + history timeline + relationships + staleness queue
12. Build `lingo` CLI — Typer; `define`, `add`, `search`, `export` subcommands; ships as `lingo` entry point in pyproject.toml
13. Docker-compose packaging + Helm chart
14. Write 5-minute quickstart README

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR | 9 proposals, 8 accepted, 1 deferred; 0 critical gaps |
| Outside Voice | `/plan-eng-review` outside voice | Independent 2nd opinion | 1 | issues_found | 10 findings: cursor granularity, Socket Mode recovery, vote lock deadlock, DEV_MODE guard, bootstrap TOCTOU, APScheduler MemoryJobStore, MCP token UX, staleness DM flood, workers enforcement, pg_trgm managed PG |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | CLEAR | Python migration: 17 issues resolved — MCP Streamable HTTP, APScheduler single-worker, Slack bolt stores, Alembic init container, SQLAlchemy session pattern, pg_trgm fuzzy, project structure, services/schemas layers, test infra, vote CAS (no SELECT FOR UPDATE), DEV_MODE guard, bootstrap TOCTOU, staleness rate limit, pg_trgm managed PG. 0 critical gaps. |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | score: 2/10 → 9/10, 14 decisions made: search-first layout, slide-in detail, design system (Tailwind+shadcn, React+Vite, Berkeley Mono), interaction states (all surfaces), responsive+a11y |

**CROSS-MODEL:** Outside voice (Claude subagent) found 10 issues — all resolved or captured as P2 TODOs. Key finding: vote SELECT FOR UPDATE deadlock vector eliminated in favor of CAS pattern.
**UNRESOLVED:** 0 unresolved decisions.
**LANGUAGE MIGRATION (2026-03-26):** Stack switched from Go to Python 3.12 + FastAPI + SQLAlchemy 2.0 + uv. Go scaffolding deleted. This Eng Review covers the Python stack.
**VERDICT:** CEO + ENG + DESIGN CLEARED — ready to implement.
